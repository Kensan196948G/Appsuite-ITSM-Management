# ============================================
# AppSuite ITSM Management System
# Production Environment Variables Template
# ============================================
#
# このファイルをコピーして .env.production として使用してください
# cp config/.env.production.template config/.env.production
#
# 注意: .env.production は .gitignore に含まれているため、
#       機密情報を含めても安全です
#
# ============================================

# ============================================
# 本番サーバー情報
# ============================================

# 本番サーバーのホスト名またはIPアドレス
PRODUCTION_HOST=appsuite-itsm.example.com

# SSH接続用ユーザー名
PRODUCTION_USER=deploy-user

# Webサーバーのドキュメントルート
PRODUCTION_PATH=/var/www/appsuite-itsm

# アプリケーションURL
PRODUCTION_URL=https://appsuite-itsm.example.com

# ============================================
# SSL証明書設定
# ============================================

# SSL証明書のパス（Let's Encryptの場合）
SSL_CERT_PATH=/etc/letsencrypt/live/appsuite-itsm.example.com/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/appsuite-itsm.example.com/privkey.pem
SSL_CHAIN_PATH=/etc/letsencrypt/live/appsuite-itsm.example.com/chain.pem

# 自己署名証明書のパス（開発環境のみ）
# SSL_CERT_PATH=/etc/ssl/certs/appsuite-itsm.crt
# SSL_KEY_PATH=/etc/ssl/private/appsuite-itsm.key

# SSL証明書情報（自己署名証明書生成時に使用）
DOMAIN=appsuite-itsm.example.com
EMAIL=admin@example.com
COUNTRY=JP
STATE=Tokyo
CITY=Chiyoda-ku
ORGANIZATION=Example Company
ORGANIZATIONAL_UNIT=IT Department

# ============================================
# バックアップ設定
# ============================================

# バックアップディレクトリ
BACKUP_DIR=/var/backups/appsuite-itsm

# バックアップ保持世代数
BACKUP_RETENTION=7

# バックアップの自動転送先（オプション）
# BACKUP_REMOTE_HOST=backup-server.example.com
# BACKUP_REMOTE_USER=backup-user
# BACKUP_REMOTE_PATH=/backups/appsuite-itsm

# ============================================
# ログ設定
# ============================================

# ログレベル（debug, info, warn, error）
LOG_LEVEL=info

# デプロイログファイル
LOG_FILE=/var/log/appsuite-itsm-deploy.log

# アクセスログ保持期間（日数）
ACCESS_LOG_RETENTION=90

# エラーログ保持期間（日数）
ERROR_LOG_RETENTION=90

# ============================================
# DeskNet's Neo API設定（オプション）
# ============================================

# API URL
API_ENDPOINT=https://your-domain.desknets.com/cgi-bin/dneo/zap.cgi

# APIキー（本番環境では環境変数から読み込み推奨）
# API_KEY=your-api-key-here

# API認証方式（bearer, basic, apikey）
API_AUTH_METHOD=bearer

# APIタイムアウト（秒）
API_TIMEOUT=30

# API自動同期間隔（分）
API_SYNC_INTERVAL=15

# ============================================
# セキュリティ設定
# ============================================

# パスワードポリシー
PASSWORD_MIN_LENGTH=12
PASSWORD_REQUIRE_UPPER=true
PASSWORD_REQUIRE_NUMBER=true
PASSWORD_REQUIRE_SPECIAL=true
PASSWORD_EXPIRY_DAYS=90

# セッション設定
SESSION_TIMEOUT_MINUTES=30
SESSION_MAX_CONCURRENT=3

# ログイン制限
LOGIN_MAX_ATTEMPTS=5
LOGIN_LOCKOUT_MINUTES=15

# ============================================
# 監視・アラート設定
# ============================================

# ヘルスチェックURL
HEALTH_CHECK_URL=https://appsuite-itsm.example.com

# ヘルスチェック間隔（分）
HEALTH_CHECK_INTERVAL=5

# アラート通知先メールアドレス
ALERT_EMAIL=admin@example.com

# CPU使用率アラート閾値（%）
ALERT_CPU_THRESHOLD=80

# メモリ使用率アラート閾値（%）
ALERT_MEMORY_THRESHOLD=90

# ディスク使用率アラート閾値（%）
ALERT_DISK_THRESHOLD=85

# SSL証明書期限アラート（日数）
ALERT_SSL_EXPIRY_DAYS=30

# ============================================
# Webサーバー設定
# ============================================

# Webサーバータイプ（apache, nginx）
WEBSERVER_TYPE=apache

# Webサーバーのユーザー
WEBSERVER_USER=www-data
WEBSERVER_GROUP=www-data

# ディレクトリ権限
DIRECTORY_PERMISSION=755

# ファイル権限
FILE_PERMISSION=644

# ============================================
# パフォーマンス設定
# ============================================

# Gzip圧縮有効化
ENABLE_GZIP=true

# ブラウザキャッシュ有効化
ENABLE_CACHE=true

# CSS/JSキャッシュ期間（日数）
CACHE_STATIC_ASSETS_DAYS=7

# 画像キャッシュ期間（日数）
CACHE_IMAGES_DAYS=30

# ============================================
# メンテナンス設定
# ============================================

# メンテナンスモード
MAINTENANCE_MODE=false

# メンテナンスメッセージ
MAINTENANCE_MESSAGE=システムのアップデートを行っています。しばらくお待ちください。

# メンテナンス完了予定時刻
# MAINTENANCE_ETA=2026-02-04 10:00

# ============================================
# 環境識別
# ============================================

# 環境名（production, staging, development）
ENVIRONMENT=production

# バージョン
VERSION=1.0.0

# デプロイ日時（自動設定）
# DEPLOY_DATE=2026-02-04T09:00:00Z

# ============================================
# 外部サービス連携（オプション）
# ============================================

# Slackウェブフック（アラート通知用）
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# メール送信設定（SMTP）
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=noreply@example.com
# SMTP_PASSWORD=your-smtp-password
# SMTP_FROM=AppSuite ITSM <noreply@example.com>

# ============================================
# データベース設定（将来の拡張用）
# ============================================

# データベースタイプ（現在はlocalStorage使用）
# DB_TYPE=mysql

# データベース接続情報
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=appsuite_itsm
# DB_USER=appsuite_user
# DB_PASSWORD=your-db-password

# ============================================
# キャッシュサーバー（将来の拡張用）
# ============================================

# Redis設定
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password

# ============================================
# 開発者向け設定
# ============================================

# デバッグモード（本番環境では必ずfalse）
DEBUG=false

# ソースマップ有効化（本番環境では必ずfalse）
SOURCE_MAPS=false

# コンソールログ出力（本番環境では必ずfalse）
CONSOLE_LOG=false

# ============================================
# 使用方法
# ============================================
#
# 1. このファイルをコピー:
#    cp config/.env.production.template config/.env.production
#
# 2. 環境に合わせて値を編集:
#    vi config/.env.production
#
# 3. スクリプトから読み込み:
#    source config/.env.production
#
# 4. 機密情報の管理:
#    - .env.production は .gitignore に含まれています
#    - GitHubにコミットされません
#    - 本番サーバー上で直接編集してください
#
# ============================================

# ============================================
# セキュリティ注意事項
# ============================================
#
# - このファイルには機密情報が含まれる可能性があります
# - Gitリポジトリにコミットしないでください
# - 適切なファイル権限を設定してください（chmod 600）
# - 定期的にAPIキー等を更新してください
# - パスワードは強固なものを使用してください
#
# ============================================
