# 🤖 GitHub Actions × Claude Code 自動修復ループ

**Appsuite-ITSM-Management** プロジェクト専用のCI自動修復システム

---

## 🎯 システム概要

このシステムは、**PowerShell特化** + **Claude Code連携** による自動ビルド修復ループです。

### 全体フロー

```
① GitHub Actions (self-hosted / Linux)
        ↓
② PowerShell ビルド実行 (pwsh ./build.ps1)
        ↓
③ 失敗 → build.log に保存
        ↓
④ Loop Guard チェック（無限ループ防止）
        ↓
⑤ Claude Code がログ解析 → 最小修正
        ↓
⑥ git commit & push
        ↓
⑦ push トリガーで再ビルド
        ↓
⑧ 成功 or 最大5回までループ
```

---

## ✅ 確定構成

| 項目 | 値 | 理由 |
|------|-----|------|
| 言語 | **PowerShell 7.x** (pwsh) | 社内IT運用DSL / Linux対応 / Claude相性◎ |
| ビルドコマンド | `pwsh ./build.ps1` | 構文 + テスト + ドライラン統合 |
| 実行環境 | **Linux self-hosted runner** | 安定性 / 自由度 / 再現性 |
| AI修復 | **Claude Code (ci-repairer)** | PowerShell特化SubAgent |
| 無限ループ防止 | **Loop Guard** | 最大5回 / 同一エラー検出 |
| 証跡保存 | **ci_logs/** | ITSM監査対応（90日保存） |

---

## 📁 ファイル構成

```
Appsuite-ITSM-Management/
├── build.ps1                                # PowerShellビルドスクリプト
├── .github/workflows/
│   ├── auto-fix-loop.yml                    # ESLint/Prettier版（既存）
│   └── auto-fix-pwsh-claude.yml             # PowerShell+Claude版（★新規）
├── .claude/agents/
│   └── ci-repairer.md                       # CI修理工SubAgent定義
├── ci/
│   ├── README.md                            # このファイル
│   ├── auto_fix_with_claudecode.sh          # Claude Code呼び出し
│   └── loop-guard.sh                        # 無限ループ防止
└── ci_logs/                                 # 証跡ログ保存（自動生成）
    ├── build_YYYYMMDD_HHMMSS.log
    ├── diff_YYYYMMDD_HHMMSS.patch
    └── claude_output_YYYYMMDD_HHMMSS.txt
```

---

## 🚀 使用方法

### 方法1: 自動実行（push時）

PowerShellファイル（*.ps1）をpushすると自動実行：

```bash
git add build.ps1
git commit -m "Update build script"
git push  # ← auto-fix-pwsh-claude.yml が自動起動
```

### 方法2: 手動実行

GitHub Actionsから手動トリガー：

1. GitHub → Actions → **Auto Fix Loop (PowerShell + Claude Code)**
2. **Run workflow** をクリック
3. Max iterations (デフォルト: 5) を指定
4. **Run workflow** 実行

### 方法3: ローカルテスト

```bash
# ビルドスクリプトを直接実行
pwsh ./build.ps1

# 失敗した場合、Claude Code 手動呼び出し
bash ./ci/auto_fix_with_claudecode.sh
```

---

## 🔍 build.ps1 の動作

### Step 1: 構文チェック（PSScriptAnalyzer）

```powershell
# すべての*.ps1ファイルを検証
Invoke-ScriptAnalyzer -Path *.ps1 -Severity Error
```

**検出されるエラー例**:
- PSAvoidUsingWriteHost
- PSUseDeclaredVarsMoreThanAssignments
- PSAvoidUsingCmdletAliases

### Step 2: 単体テスト（Pester）

```powershell
# tests/*.Tests.ps1 を実行
Invoke-Pester -Path ./tests -EnableExit
```

### Step 3: JavaScript 検証（ESLint）

```bash
npx eslint WebUI-Sample/js/*.js --format compact
```

### Step 4: メイン処理ドライラン

```powershell
# 将来の拡張用（現在は未実装）
./Main.ps1 -DryRun
```

---

## 🤖 Claude Code CI Repairer

### SubAgent定義

`.claude/agents/ci-repairer.md` に定義された専門AI：

**役割**:
- ビルドログ解析
- PowerShell構文エラー修復
- Pesterテスト失敗修正
- ESLintエラー修正

**ルール**:
- ❌ 設計変更禁止
- ❌ 大規模リファクタ禁止
- ✅ **最小diff** のみ
- ✅ エラー箇所のみ修正

### 修復プロセス

```
1. build.log を全文読み込み
2. エラー種類を特定（PSScriptAnalyzer / Pester / ESLint）
3. エラー発生箇所（ファイル:行）を特定
4. 最小変更で修正
5. 変更をコミット
```

### 出力フォーマット

```json
{
  "status": "FIXED | ABORTED | PARTIAL",
  "summary": "修正内容の要約",
  "fixes_applied": [
    {"file": "build.ps1", "line": 42, "fix": "Write-Host → Write-Information"}
  ],
  "files_modified": ["build.ps1"],
  "next_action": "COMMIT | MANUAL_REVIEW"
}
```

---

## 🛡️ Loop Guard（無限ループ防止）

### 防止機構

| チェック項目 | 閾値 | アクション |
|------------|------|----------|
| 最大試行回数 | 5回 | ABORT → Issue作成 |
| 同一エラー繰り返し | 2回連続 | ABORT → 手動レビュー要求 |
| 連続失敗 | 3回 | WARNING表示 |

### 状態ファイル

- `.ci_attempt` - 現在の試行回数
- `.ci_error_hash` - 前回のエラーハッシュ
- `.ci_consecutive_failures` - 連続失敗回数

### リセット方法

```bash
# ループガードをリセット
rm -f .ci_attempt .ci_error_hash .ci_consecutive_failures
```

---

## 📊 証跡ログ（ITSM対応）

### 保存内容

すべてのCI実行結果は `ci_logs/` に保存：

```
ci_logs/
├── build_20260123_143022.log       # ビルドログ
├── diff_20260123_143022.patch      # Git差分
└── claude_output_20260123_143022.txt  # Claude Code出力
```

### 保存期間

- **GitHub Actions**: 90日（アーティファクト）
- **ローカル**: 手動管理（推奨: 1年）

### 監査対応

- ISO20000 / ISO27001 対応
- 「誰が」「いつ」「何を」修正したかトレース可能
- Issue自動作成で障害履歴を記録

---

## 🔧 運用ポリシー

### Claude Code の役割

- ✅ CI修理工に限定
- ❌ 設計変更・大規模リファクタは禁止
- ✅ minimal diff 優先
- ❌ .ps1 ファイル以外は原則触らない

### エラー対応優先順位

1. **PSScriptAnalyzer エラー** - 構文違反（最優先）
2. **Pester テスト失敗** - ロジックエラー
3. **ESLint エラー** - JavaScript構文
4. **その他** - ABORT して人間に通知

### 手動介入が必要なケース

以下の場合は自動修復を中止：

- 同じエラーが2回連続
- 最大5回試行後も失敗
- 設計変更が必要
- 外部依存の問題（API/ネットワーク）

---

## 📈 運用フロー図

```
┌─────────────────────────────────────────┐
│ Developer pushes code                   │
└───────────────┬─────────────────────────┘
                ↓
┌─────────────────────────────────────────┐
│ GitHub Actions triggered                │
└───────────────┬─────────────────────────┘
                ↓
        ┌───────────────┐
        │ pwsh build.ps1│
        └───────┬───────┘
                ↓
      ┌─────────┴──────────┐
      │                    │
  SUCCESS                FAILURE
      ↓                    ↓
 ┌────────┐         ┌──────────────┐
 │ Clean  │         │ Loop Guard?  │
 │ Up     │         └──────┬───────┘
 └────────┘                ↓
                   ┌───────┴────────┐
                   │                │
                PASS             FAIL
                   ↓                ↓
          ┌────────────────┐  ┌─────────┐
          │ Claude Code    │  │ Create  │
          │ Auto-Fix       │  │ Issue   │
          └───────┬────────┘  └─────────┘
                  ↓
          ┌────────────────┐
          │ git commit     │
          │ & push         │
          └───────┬────────┘
                  ↓
          ┌────────────────┐
          │ Re-trigger     │
          │ (loop back)    │
          └────────────────┘
```

---

## 🧪 テストケース

### Case 1: PSScriptAnalyzer エラー

**問題**:
```powershell
Write-Host "Hello"  # PSAvoidUsingWriteHost
```

**自動修復後**:
```powershell
Write-Information "Hello"
```

### Case 2: Pester テスト失敗

**問題**:
```powershell
$result | Should -Be 43  # 間違い
```

**自動修復後**:
```powershell
$result | Should -Be 42  # 修正
```

### Case 3: ESLint エラー

**問題**:
```javascript
var x = 10;  // no-var
```

**自動修復後**:
```javascript
const x = 10;
```

---

## 🚨 トラブルシューティング

### Q1: Workflowが起動しない

**原因**: self-hosted runner が停止している

**解決策**:
```bash
# Linux self-hosted runner の状態確認
systemctl status actions.runner.*
```

### Q2: Claude Code が動作しない

**原因**: Claude Code CLI が未インストール

**解決策**:
- このスクリプトは Claude Code **から** 呼び出される想定
- 手動実行時は `ci/auto_fix_with_claudecode.sh` が manual review fileを生成

### Q3: 無限ループが止まらない

**原因**: Loop Guard が機能していない

**解決策**:
```bash
# 強制リセット
rm -f .ci_attempt .ci_error_hash .ci_consecutive_failures

# Workflow を手動停止
# GitHub → Actions → Cancel workflow
```

### Q4: 証跡ログが多すぎる

**原因**: 長期間蓄積

**解決策**:
```bash
# 30日以上前のログを削除
find ci_logs/ -name "*.log" -mtime +30 -delete
```

---

## 🔄 今後の拡張

### Phase 1: 現在（完成）
- ✅ PowerShell + Claude Code 自動修復
- ✅ 無限ループ防止
- ✅ 証跡ログ保存

### Phase 2: 将来
- [ ] Slack/Teams通知連携
- [ ] カスタムPSScriptAnalyzerルール
- [ ] パフォーマンステスト追加
- [ ] メトリクス可視化（Grafana）

---

## 📚 参考資料

- [PowerShell 7 ドキュメント](https://learn.microsoft.com/ja-jp/powershell/)
- [PSScriptAnalyzer](https://github.com/PowerShell/PSScriptAnalyzer)
- [Pester](https://pester.dev/)
- [GitHub Actions self-hosted runner](https://docs.github.com/ja/actions/hosting-your-own-runners)

---

**Version**: 1.0
**Last Updated**: 2026-01-23
**Author**: Claude Sonnet 4.5 (1M context)
**Project**: Appsuite-ITSM-Management
